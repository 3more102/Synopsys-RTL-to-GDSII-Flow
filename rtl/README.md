# RTL input
Place the project Verilog/SystemVerilog source files here. `config/rtl_files.tcl` discovers `.v` and `.sv` files deterministically. The configured top module is `mips_16` by default and can be overridden with `TOP_MODULE`.
