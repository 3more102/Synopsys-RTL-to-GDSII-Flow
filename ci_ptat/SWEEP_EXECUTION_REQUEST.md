# PTAT SKY130 design-sweep execution

This CI-only marker triggers the branch workflow that runs the full TT/FF/SS × VDD × bias design sweeps and dense validation of the selected low-power candidate. It is part of the temporary draft sandbox and must not be merged to `main`.
