# Library inputs
Configure the logical `.db` target library and physical `.ndm` reference library through `TARGET_LIBRARY` and `STD_CELL_NDM`. Library filenames and locations are intentionally not guessed.
