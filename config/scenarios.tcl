# Conceptual scenarios; scripts do not fabricate missing corner libraries.
set SCENARIOS [dict create \
    func_setup [dict create mode functional corner SS enabled 0] \
    func_hold  [dict create mode functional corner FF enabled 0]]
